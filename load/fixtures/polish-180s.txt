嗯，我想确认一下这次语音输入服务的部署方案。服务器是两核四 GB 内存，先使用单个应用实例，不启用 Redis。我们需要分别测量同时录音用户数、语音识别工作位和文本整理工作位。测试时请记录每个阶段的排队时间、处理时间、成功率、重试次数、CPU、内存以及网络收发速度。如果上游服务返回限流、超时或者服务器错误，即使重试后成功，也要把这一轮标记为严格失败。最终报告需要区分测试通过上限和保留余量后的生产推荐值，并说明失败来自本地资源、队列、网络，还是模型服务商。
